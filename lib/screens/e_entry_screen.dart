import 'package:flutter/material.dart';

class eEntryScreen extends StatelessWidget {
  const eEntryScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    print('Test');

    return Center(child: Text('eEntry'));
  }
}
